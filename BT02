#include <iostream>

using namespace std;

bool Prime(unsigned int n)
{
    for (int i=2;i<n-1;i++)
        if(n%i==0) return false;
    return true;
}

int main()
{
    int x; cin >> x;
    if(Prime(x)) cout << "yes" << endl;
    else cout << "no" << endl;
    return 0;
}
